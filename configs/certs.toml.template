[[tls.certificates]]
certFile = "/etc/ssl/{{CERT_FILE}}"
keyFile = "/etc/ssl/{{KEY_FILE}}"
stores = ["default"]

[tls.stores]
[tls.stores.default]
[tls.stores.default.defaultCertificate]
certFile = "/etc/ssl/{{CERT_FILE}}"
keyFile = "/etc/ssl/{{KEY_FILE}}"
